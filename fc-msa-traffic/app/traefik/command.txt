# traefik
traefik --configFile=./traefik/traefik.yml

# container
docker run -d \
  --name traefik \
  -p 8080:8080 \
  -p 5052:5052 \
  -v "$(pwd)/traefik.yml:/etc/traefik/traefik.yml" \
  -v "$(pwd)/rules:/rules" \
  traefik:v3.4.4
